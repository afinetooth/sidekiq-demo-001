<%= form_with(model: sku, local: true) do |form| %>
  <% if sku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sku.errors.count, "error") %> prohibited this sku from being saved:</h2>

      <ul>
      <% sku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :style_id %>
    <%= form.number_field :style_id, id: :sku_style_id %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size, id: :sku_size %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :sku_price %>
  </div>

  <div class="field">
    <%= form.label :upc %>
    <%= form.text_field :upc, id: :sku_upc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
